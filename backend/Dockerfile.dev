FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose ports
EXPOSE 3000 3001

# Development command (will be overridden by docker-compose)
CMD ["npm", "run", "dev"]
